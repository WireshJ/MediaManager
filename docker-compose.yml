services:
  mediamanager:
    build: .
    container_name: mediamanager
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      # Bewaar instellingen en data buiten de container
      - ./data:/app/data
    environment:
      - PORT=8080
